#include "stdafx.h"
#include "Player.h"
#include <Keyboard.hpp>
#include <Event.hpp>

Player::Player()
{
}


Player::~Player()
{
}

sf::Event event;


void Player::controlEvent(unsigned char key, static bool isKeyPressed) {

	if (isKeyPressed) {
		switch(key)
			case "a":

	}

}